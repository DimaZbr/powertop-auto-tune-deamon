# powertop-auto-tune-deamon
This daemon is a systemd unit that automatically applies the --auto-tune command when your device boots. This improves the power optimization of the entire device. It is much more reliable than entering it manually after every reboot.
